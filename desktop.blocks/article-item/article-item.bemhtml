block article-item {

    elem title {
        tag: 'h3'
    }

    elem link-to-all {
        tag: 'span',
        content: { block: 'b-link', content: this.ctx.content, url: this.ctx.url }
    }

    elem link {
        content: { block: 'b-link', content: this.ctx.content, url: this.ctx.url }
    }

    elem header {
        mix: [ { block: 'i-font', mods: { face: 'hermes' } } ]
    }
}