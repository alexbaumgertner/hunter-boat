block buy-now-list {

    elem list, tag: 'ul'

    elem item {
        tag: 'li',
        content: {
            block: 'b-link',
            url: this.ctx.url,
            content: this.ctx.name
        }
    }
}