block buyer-menu {

    tag: 'ul'

    elem item {

        tag: 'li'
        content: {
            block: 'b-link',
            content: this.ctx.name,
            url: this.ctx.url
        }
    }

    elem item, elemMod state current {
        tag: 'li',
        content: this.ctx.name
    }

}