block photoalbums-carousel {

    mix: { block: 'i-carousel', mods: { type: 'horiz' } },
    js: 'true'

    elem list, tag: 'ul'

    elem item, tag: 'li'

    elem header {
        mix: [ { block: 'i-font', mods: { face: 'hermes' } } ]
    }
}