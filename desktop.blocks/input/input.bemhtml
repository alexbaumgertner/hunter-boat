block input {
    tag: 'input',
    attrs: {
        placeholder: this.ctx.placeholder,
        name: this.ctx.name,
        size: this.ctx.size,
        type: this.ctx.type,
        value: this.ctx.value
    }
}